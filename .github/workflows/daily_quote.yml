name: daily quote

permissions:
    contents: write

on:
    schedule:
        - cron: '30 0 * * *'

jobs:
    run:
        runs-on: ubuntu-latest

        steps:
            - run: pip install requirements.txt
            - run: python daily_quote.py